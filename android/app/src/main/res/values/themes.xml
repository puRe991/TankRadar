<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the launch theme; everything after first frame is Compose. -->
    <style name="Theme.TankRadar" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/tankradar_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
