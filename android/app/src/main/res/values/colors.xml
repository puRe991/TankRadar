<?xml version="1.0" encoding="utf-8"?>
<!--
    Only the colours the Android resource system needs: the launch theme's window
    background and the adaptive-icon background. Everything the UI paints lives in
    ui/theme/Theme.kt, because Compose reads Kotlin values, not resources.
-->
<resources>
    <color name="tankradar_background">#07111F</color>
</resources>
